/**
 * Altura fija del header en todas las pantallas.
 * Garantiza que el ThemeToggle quede en la misma posición
 * al navegar entre home y las pantallas internas.
 *
 * Cálculo: ThemeToggle (44px) + paddingVertical (20 × 2) = 84px
 */
export const HEADER_HEIGHT = 84;

/**
 * Padding horizontal estándar de la app.
 * Aplicado en headers, listas, inputs y contenedores de contenido.
 */
export const SPACING = 20;
